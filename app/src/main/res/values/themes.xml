<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Framework Material theme so no AppCompat/Material-Components XML
         dependency is needed; the actual UI is Jetpack Compose which themes
         itself. NoActionBar because Compose draws its own top bars. -->
    <style name="Theme.NotificationFirewall" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
