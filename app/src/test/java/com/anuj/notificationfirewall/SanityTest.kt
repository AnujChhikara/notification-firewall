package com.anuj.notificationfirewall

import org.junit.Assert.assertEquals
import org.junit.Test

class SanityTest {
    @Test
    fun toolchain_runs_headless_unit_tests() = assertEquals(4, 2 + 2)
}
